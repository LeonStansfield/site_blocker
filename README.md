# Site Blocker

A Firefox extension that blocks configured websites on a schedule. Solve a short puzzle to unlock access for one hour.

## Install

Open `about:debugging#/runtime/this-firefox`, choose **Load Temporary Add-on**, and select `manifest.json`.